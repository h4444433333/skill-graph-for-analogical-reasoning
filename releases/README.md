# Release Assets

This directory keeps publish-facing release assets for `Skill Graph for Analogical Reasoning`.

## GitHub version

The repository root remains the main working source tree.
This folder now also includes a visible GitHub-facing snapshot under:

- `github/skill-graph-for-analogical-reasoning/`

That snapshot includes:

- full Python package source
- CLI implementation
- top-level README
- `pyproject.toml`
- local skill files

## What is in this folder?

- `clawhub-skill/` — final ClawHub skill bundle for **Skill Graph for Analogical Reasoning**
- `github/` — visible GitHub-facing source snapshot
- `gist/` — long-form public markdown writeup, useful for a gist/article page

The ClawHub bundle is code-backed:

- it includes `SKILL.md`
- it includes `scripts/run_grap_skill.py`
- it includes `python-package/src/auto_grap_skill/`
- it is a code-backed skill bundle, not markdown-only documentation

## Naming

Public title:

- **Skill Graph for Analogical Reasoning**

Current local CLI command:

- `grap-skill`
